<article id="{{num}}" class="slide {{slide.class}}">
  <header>
  </header>
  <section class="content">
    {{{html}}}
  </section>
  <footer>
    <span class="left">{{show.title}}</span>
    <span class="center">{{show.author}}</span>
    <span class="right">{{num}}/{{total}}</span>
  </footer>
</article>